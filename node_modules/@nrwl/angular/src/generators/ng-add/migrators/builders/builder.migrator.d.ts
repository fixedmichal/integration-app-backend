import type { ProjectConfiguration, TargetConfiguration, Tree } from '@nrwl/devkit';
import type { Logger, ProjectMigrationInfo } from '../../utilities';
import { Migrator } from '../migrator';
export declare abstract class BuilderMigrator extends Migrator {
    readonly builderName: string;
    protected targets: Map<string, TargetConfiguration>;
    constructor(tree: Tree, builderName: string, project: ProjectMigrationInfo, projectConfig: ProjectConfiguration, logger: Logger);
    protected collectBuilderTargets(): void;
}
